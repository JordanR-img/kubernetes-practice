apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend-deployment-controller
  labels:
    app: myapp
    type: front-end
spec:
  template:
    metadata:
      name: nginx
      labels:
        app: myapp-deployment
        type: front-end
    spec:
      containers:
        - name: nginx
          image: nginx

  replicas: 3
  selector:
    matchLabels:
      app: myapp-deployment
      type: front-end
      
